export type Mapping = {
    message?: string
    error?: string
    value: boolean
}

export type Data = {
    chainId?: number
    emailHash?: string
    sender?: string
    message?: string
    timestamp?: number
    error?: string
  };